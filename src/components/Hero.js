const Hero = () => {
    return (
        <header className="pf-hero">
            <p className="title">Pattern Friend</p>
            <p className="subtitle">Extend the Default</p>
        </header>
    );
}

export default Hero;

